apiVersion: v1
kind: Service
metadata:
  name: dst-dashboard-backend
spec:
  selector:
    app: dst-dashboard-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
  clusterIP: None